### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb') #doesn't show folder route
class CardGame


  def checkforAce(card) #should be all lower case with underscores
    if card.value = 1
      return true
    else
      return false
    end
  end

  dif highest_card(card1 card2) #should be def, needs comma between params
  if card1.value > card2.value
    return card.name #doesn't specify which card
  else
    card2   # ideally would use return statement
  end
end
end

def self.cards_total(cards)
  total    # need to assign initial amount to total
  for card in cards
    total += card.value
    return "You have a total of" + total
  end
end


```
